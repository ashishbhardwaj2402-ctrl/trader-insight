# Frontend boundary

Reserved for the Trader Insight desktop client and its client-side tooling.

Frontend code may consume published or derived contract types, but must not import Python backend source, CDK infrastructure, Lambda handlers, or AWS SDK implementation code. No application bootstrap or dashboard behavior is included in this scaffold.
